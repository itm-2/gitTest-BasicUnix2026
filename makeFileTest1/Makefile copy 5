cc = gcc
CFLAGS = -Wall -MMD # Generate dependency files / include header files in the dependency list

SRC = main.c add.c
OBJ = main.o add.o
DEP = $(OBJ:.o=.d) # main.d add.d

.PHONY: all clean

app: $(OBJ)
	$(cc) $(CFLAGS) $^ -o $@

#%.o: %.c %.h
#	$(cc) $(CFLAGS) -c $< -o $@

clean:
	rm -rf app *.o

helloTest: abc def
	echo $^
abc:
	@echo abc
# @ 빼면 echo abc도 출력되고, abc도 출력됨.
def:
	@echo def